#include <stdio.h>
#include <stdlib.h>
#include "header.h"

struct Puzzle initializeMenu(struct Puzzle sudoku)
{
    int again = 1;
    int error = 0;
    do
    {
        system("cls");

    }
    while(again);
};
